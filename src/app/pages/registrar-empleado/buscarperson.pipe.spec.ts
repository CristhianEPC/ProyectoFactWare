import { BuscarpersonPipe } from './buscarperson.pipe';

describe('BuscarpersonPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarpersonPipe();
    expect(pipe).toBeTruthy();
  });
});
