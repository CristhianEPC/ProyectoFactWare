import { BuscarproducventPipe } from './buscarproducvent.pipe';

describe('BuscarproducventPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarproducventPipe();
    expect(pipe).toBeTruthy();
  });
});
