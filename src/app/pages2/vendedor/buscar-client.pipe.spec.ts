import { BuscarClientPipe } from './buscar-client.pipe';

describe('BuscarClientPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarClientPipe();
    expect(pipe).toBeTruthy();
  });
});
